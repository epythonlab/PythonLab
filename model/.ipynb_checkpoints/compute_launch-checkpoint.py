def days_until_launch(current_day,launch_day):
    """Returns the days left befor launch
    current_day(int) - current day in integer
    launch_day (int) - launch  day in integer"""
    
    return launch_day - current_day